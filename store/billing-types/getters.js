export default {
    billingTypes(state) {
        return state.billingTypes;
    },
    totalBillingType(state) {
        return state.totalBillingType;
    },
    selectedBillingType(state) {
        return state.selectedBillingType;
    },
    billingTypeFile(state) {
        return state.billingTypeFile;
    }
}