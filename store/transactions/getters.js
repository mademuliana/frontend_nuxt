export default {
    transactions(state) {
        return state.transactions;
    },
    totalTransaction(state) {
        return state.totalTransaction;
    },
    selectedTransaction(state) {
        return state.selectedTransaction;
    },
    transactionFile(state) {
        return state.transactionFile;
    }
}