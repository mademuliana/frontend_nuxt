export default {
    setProvincies(state, provincies) {
        state.provincies = provincies;
    },
    setTotalProvince(state, totalProvince) {
        state.totalProvince = totalProvince;
    },
    setSelectedProvince(state, province) {
        state.selectedProvince = province;
    },
    setProvinceFile(state, provinceFile) {
        state.provinceFile = provinceFile;
    }
}