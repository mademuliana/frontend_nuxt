export default {
    setRanges(state, ranges) {
        state.ranges = ranges;
    },
    setTotalRange(state, totalRange) {
        state.totalRange = totalRange;
    },
    setSelectedRange(state, range) {
        state.selectedRange = range;
    },
    setRangeFile(state, rangeFile) {
        state.rangeFile = rangeFile;
    }
}