export default {
    ranges(state) {
        return state.ranges;
    },
    totalRange(state) {
        return state.totalRange;
    },
    selectedRange(state) {
        return state.selectedRange;
    },
    rangeFile(state) {
        return state.rangeFile;
    }
}