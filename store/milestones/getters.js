export default {
    milestones(state) {
        return state.milestones;
    },
    totalMilestone(state) {
        return state.totalMilestone;
    },
    selectedMilestone(state) {
        return state.selectedMilestone;
    },
    milestoneFile(state) {
        return state.milestoneFile;
    }
}