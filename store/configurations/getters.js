export default {
    configurations(state) {
        return state.configurations;
    },
    totalConfiguration(state) {
        return state.totalConfiguration;
    },
    selectedConfiguration(state) {
        return state.selectedConfiguration;
    },
    configurationFile(state) {
        return state.configurationFile;
    }
}