export default {
    orders(state) {
        return state.orders;
    },
    totalOrder(state) {
        return state.totalOrder;
    },
    selectedOrder(state) {
        return state.selectedOrder;
    },
    orderFile(state) {
        return state.orderFile;
    }
}