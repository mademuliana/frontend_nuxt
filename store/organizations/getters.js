export default {
    organizations(state) {
        return state.organizations;
    },
    totalOrganization(state) {
        return state.totalOrganization;
    },
    selectedOrganization(state) {
        return state.selectedOrganization;
    },
    organizationFile(state) {
        return state.organizationFile;
    }
}