export default {
    setChargeCodes(state, chargeCodes) {
        state.chargeCodes = chargeCodes;
    },
    setTotalChargeCode(state, totalChargeCode) {
        state.totalChargeCode = totalChargeCode;
    },
    setSelectedChargeCode(state, chargeCode) {
        state.selectedChargeCode = chargeCode;
    },
    setChargeCodeFile(state, chargeCodeFile) {
        state.chargeCodeFile = chargeCodeFile;
    }
}