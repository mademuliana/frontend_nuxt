export default {
    chargeCodes(state) {
        return state.chargeCodes;
    },
    totalChargeCode(state) {
        return state.totalChargeCode;
    },
    selectedChargeCode(state) {
        return state.selectedChargeCode;
    },
    chargeCodeFile(state) {
        return state.chargeCodeFile;
    }
}