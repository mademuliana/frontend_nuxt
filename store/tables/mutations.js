export default {
    setTables(state, tables) {
        state.tables = tables;
    },
    setTotalTable(state, totalTable) {
        state.totalTable = totalTable;
    },
    setSelectedTable(state, table) {
        state.selectedTable = table;
    },
    setTableFile(state, tableFile) {
        state.tableFile = tableFile;
    }
}