export default {
    tables(state) {
        return state.tables;
    },
    totalTable(state) {
        return state.totalTable;
    },
    selectedTable(state) {
        return state.selectedTable;
    },
    tableFile(state) {
        return state.tableFile;
    }
}