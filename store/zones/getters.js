export default {
    zones(state) {
        return state.zones;
    },
    totalZone(state) {
        return state.totalZone;
    },
    selectedZone(state) {
        return state.selectedZone;
    },
    zoneFile(state) {
        return state.zoneFile;
    }
}